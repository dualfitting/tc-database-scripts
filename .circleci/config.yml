version: 2

jobs:
  build:
    machine: true
    environment: 
      DOCKER_REPO: appiriodevops/tc-database-scripts
    steps:
      - checkout
      - setup_remote_docker
      - run: .deploy/dockerpush.sh

workflows:
  version: 2
  build-and-deploy:
    jobs:
      - build:
          filters:
            branches:
              # FIXME: The original is master, set dev for test
              only: dev